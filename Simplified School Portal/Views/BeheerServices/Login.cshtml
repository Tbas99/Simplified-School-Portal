@using Simplified_School_Portal.Models
@model LoginViewModel
@{
    Layout = null;
    ViewBag.Title = "Please sign in!";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Beheerders log-in - Simplified School Portal</title>
    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/logincss")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <div class="container">
        <div class="wrapper">
            @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-signin", role = "form", name = "Login_Form" }))
            {

                @Html.AntiForgeryToken()
                <h3 class="form-signin-heading">@ViewBag.Title</h3>
                <hr class="colorgraph"><br>

                <div class="form-group">
                    @Html.TextBoxFor(m => m.Email, new { @class = "form-control", @type = "email", @name = "Username", @placeholder = "Email", @required = "required" })
                </div>

                <div class="form-group">
                    @Html.PasswordFor(m => m.Password, new { @class = "form-control", @type = "password", @name = "Password", @placeholder = "Password", @required = "required" })
                </div>

                <div class="form-group">
                    <button type="submit" class="btn btn-default btn-block" value="Contact">Submit</button>
                </div>
            }
        </div>
    </div>
</body>
</html>

<!--
    Admin user for the site:
    Email: admin@fhict.nl
    Password: Fhictportalmanager123!
-->
